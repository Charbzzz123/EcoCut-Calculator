# Shared library

Put cross-feature components, directives, pipes, and domain helpers here so features stay focused. Keep files co-located by domain (e.g., ui/, util/, models/) and expose only stable APIs.
